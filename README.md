# This is a cool  project

My project is very cool

## Setup

YOu can start the project with start command

## Building steps

1. Install nodejs: `npm i -g node`
2. Install npx: `npm i npx`
3. Install dependencies: `npn install`